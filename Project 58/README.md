## Redis (redis) caching with Express (JS)

Basic implementation of the Redis-based Node.js caching layer.
And  Caching layer with compression/decompression option to save memory.

#### Start

```
npm start
```
